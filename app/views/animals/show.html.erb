<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-4">
      <% @animals.each do |animal| %>
      <div class="card">
        animal.name
        animal.bio
        animal.price
        animal.photo[0]
      </div>
      <% end %>
    </div>
  </div>
</div>
